# SMTP Relay Configuration (via Firewall)
# ===================================

# SMTP Server (default: 192.168.98.1)
SMTP_HOST=192.168.98.1

# SMTP Port (default: 25)
SMTP_PORT=25

# Email address to send from
MAIL_FROM=pm-notification@example.com

# Server Port (optional, default: 5005)
PORT=5005
