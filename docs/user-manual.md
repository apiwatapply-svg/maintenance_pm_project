<div align="center">

# 📖 คู่มือการใช้งาน (User Manual)

## ระบบบริหารจัดการการบำรุงรักษาเชิงป้องกัน
## (Preventive Maintenance Management System)

---

**ชื่อโปรเจค:** Maintenance PM Project  
**เวอร์ชัน:** 1.0.0  
**วันที่จัดทำ:** 10 มกราคม 2569 (2026)  
**จัดทำโดย:** ทีมพัฒนาระบบ

---

</div>

---

## 📋 ข้อมูลเอกสาร

| รายการ | รายละเอียด |
|--------|-----------|
| **ชื่อเอกสาร** | คู่มือการใช้งาน (User Manual) |
| **รหัสเอกสาร** | PM-DOC-UM-001 |
| **เวอร์ชัน** | 1.0.0 |
| **วันที่จัดทำ** | 10 มกราคม 2569 |
| **ปรับปรุงล่าสุด** | 10 มกราคม 2569 |
| **สถานะ** | เผยแพร่ (Released) |
| **กลุ่มผู้ใช้** | ผู้ใช้งานทั่วไป (Staff/Operator) |

---

## 📝 ประวัติการแก้ไข

| เวอร์ชัน | วันที่ | ผู้แก้ไข | รายละเอียดการแก้ไข |
|---------|--------|---------|-------------------|
| 1.0.0 | 10/01/2569 | ทีมพัฒนา | เอกสารฉบับแรก - ครอบคลุมทุก Feature |

---

## สารบัญ

1. [บทนำ](#1-บทนำ)
2. [การเข้าสู่ระบบ (Login)](#2-การเข้าสู่ระบบ-login)
3. [หน้า Dashboard](#3-หน้า-dashboard)
4. [การจัดการเครื่องจักร (Machines)](#4-การจัดการเครื่องจักร-machines)
5. [การทำ PM (PM Inspection)](#5-การทำ-pm-pm-inspection)
6. [ประวัติ PM (PM History)](#6-ประวัติ-pm-pm-history)
7. [ปฏิทิน PM (Calendar)](#7-ปฏิทิน-pm-calendar)
8. [รายงาน (Reports)](#8-รายงาน-reports)
9. [การวิเคราะห์ (Analysis)](#9-การวิเคราะห์-analysis)
10. [คำถามที่พบบ่อย (FAQ)](#10-คำถามที่พบบ่อย-faq)

---

## 1. บทนำ

### 1.1 เกี่ยวกับระบบ

ระบบ **Preventive Maintenance Management System (PM System)** เป็นระบบบริหารจัดการการบำรุงรักษาเชิงป้องกันสำหรับเครื่องจักรในโรงงาน โดยมีความสามารถหลักดังนี้:

| Feature | คำอธิบาย |
|---------|---------|
| ✅ **Dashboard Real-time** | แสดงสถานะเครื่องจักรทั้งหมดแบบ Real-time พร้อม Auto-refresh |
| ✅ **PM Inspection** | บันทึกผลการทำ PM พร้อมระบบ Checklist, ถ่ายรูปก่อน/หลัง |
| ✅ **PM History** | ดูประวัติ PM ย้อนหลังพร้อม Export Excel/PDF |
| ✅ **Calendar View** | ปฏิทินแสดงกำหนดการ PM ทั้งเดือน |
| ✅ **Reports & Analysis** | รายงานและวิเคราะห์ข้อมูล PM |
| ✅ **Reschedule** | เลื่อนกำหนด PM สำหรับผู้มีสิทธิ์ |

### 1.2 ความต้องการของระบบ

| รายการ | ความต้องการ |
|--------|-----------|
| **Browser** | Google Chrome 90+, Microsoft Edge 90+ |
| **อุปกรณ์** | คอมพิวเตอร์, Tablet, Smartphone |
| **การเชื่อมต่อ** | Intranet หรือ VPN |
| **กล้อง** | สำหรับถ่ายรูป PM (Optional) |

### 1.3 ประเภทผู้ใช้งานและสิทธิ์

| ประเภท | System Role | Permission Type | สิทธิ์การใช้งาน |
|--------|------------|-----------------|----------------|
| **ผู้ดูแลระบบ** | ADMIN | ALL | เข้าถึงได้ทุกฟังก์ชัน |
| **ผู้ปฏิบัติงาน PM** | USER | PM_ONLY | ทำ PM ได้อย่างเดียว |
| **ผู้เลื่อนกำหนด** | USER | RESCHEDULE_ONLY | เลื่อนกำหนด PM ได้อย่างเดียว |
| **ผู้ปฏิบัติงานเต็มสิทธิ์** | USER | PM_AND_RESCHEDULE | ทำ PM และเลื่อนกำหนดได้ |

---

## 2. การเข้าสู่ระบบ (Login)

### 2.1 URL การเข้าถึง

```
http://localhost:3000/login
หรือ
https://{server-ip}:5007/login
```

### 2.2 หน้าจอ Login

หน้าจอ Login ประกอบด้วย:

| องค์ประกอบ | คำอธิบาย |
|-----------|---------|
| 👤 **Username** | ช่องกรอกชื่อผู้ใช้ |
| 🔒 **Password** | ช่องกรอกรหัสผ่าน |
| 🔵 **Sign In Button** | ปุ่มเข้าสู่ระบบ |

### 2.3 ขั้นตอนการเข้าสู่ระบบ

1. เปิด Browser แล้วไปที่ URL ของระบบ
2. กรอก **Username** ในช่องแรก
3. กรอก **Password** ในช่องที่สอง
4. กดปุ่ม **"Sign In"**
5. หากสำเร็จ จะแสดง Toast "Signed in successfully" และเข้าสู่หน้า Dashboard
6. หากล้มเหลว จะแสดง Popup แจ้งเตือน เช่น "Invalid credentials"

### 2.4 การแก้ไขปัญหาการ Login

| ปัญหา | สาเหตุที่เป็นไปได้ | วิธีแก้ไข |
|-------|-----------------|---------|
| "Invalid credentials" | Username หรือ Password ไม่ถูกต้อง | ตรวจสอบการพิมพ์ ตัวพิมพ์เล็ก-ใหญ่ |
| "Connection Error" | ไม่สามารถเชื่อมต่อ Server ได้ | ตรวจสอบ Backend Server ทำงานอยู่หรือไม่ |
| Certificate Error | ปัญหา SSL Certificate | คลิกลิงก์ Accept Certificate ที่แสดง |

### 2.5 การออกจากระบบ (Logout)

1. คลิกที่ชื่อผู้ใช้หรือไอคอนผู้ใช้ที่มุมขวาบน
2. เลือก **"ออกจากระบบ"** หรือ **"Logout"**
3. ระบบจะกลับไปหน้า Login

---

## 3. หน้า Dashboard

### 3.1 URL การเข้าถึง

```
http://localhost:3000/
```

### 3.2 ภาพรวมหน้า Dashboard

Dashboard เป็นหน้าหลักของระบบ แสดงข้อมูลสรุปสถานะเครื่องจักรทั้งหมด

### 3.3 ส่วนประกอบของ Dashboard

#### 3.3.1 Header Section

| องค์ประกอบ | ตำแหน่ง | คำอธิบาย |
|-----------|--------|---------|
| **Title "Dashboard"** | ซ้ายบน | หัวข้อหน้า |
| **Auto-refresh Timer** | ขวาบน | แสดงเวลาถอยหลังก่อน Refresh อัตโนมัติ (5 นาที) |
| **🔔 Notification Icon** | ขวาบน | แสดงการแจ้งเตือน PM ที่ใกล้ถึง/เลยกำหนด |
| **🔄 Refresh Button** | ขวาบน | กดเพื่อ Refresh ข้อมูลทันที |

#### 3.3.2 Filter Section (กล่องกรองข้อมูล)

ตัวกรองที่มีให้ใช้:

| ตัวกรอง | คำอธิบาย | หมายเหตุ |
|--------|---------|---------|
| **Area** | กรองตามพื้นที่ เช่น CLASS100, OUTDOOR | เลือกก่อน Type |
| **Type** | กรองตามประเภทเครื่อง เช่น LSM, VNS | ต้องเลือก Area ก่อน |
| **Machine** | กรองตามชื่อเครื่อง | ต้องเลือก Type ก่อน |
| **PM Type** | กรองตามประเภท PM เช่น Weekly PM | อิสระจากตัวกรองอื่น |

> **💡 เคล็ดลับ:** ตัวกรองจะถูกจดจำใน Browser คุณไม่ต้องตั้งค่าใหม่ทุกครั้งที่เข้าระบบ

#### 3.3.3 KPI Cards (6 การ์ด)

| การ์ด | สี | ความหมาย |
|------|---|---------|
| **Total Machines** | 🔵 น้ำเงิน | จำนวนเครื่องจักรทั้งหมดที่กรองได้ |
| **Total PM** | 🔷 ฟ้า | จำนวนรายการ PM ทั้งหมด |
| **Completed** | 🟢 เขียว | จำนวน PM ที่ดำเนินการเรียบร้อย |
| **Checked (NG)** | 🔴 แดง | จำนวนเครื่องที่ PM ล่าสุดมีรายการไม่ผ่าน |
| **Upcoming** | 🟡 เหลือง | จำนวน PM ที่ใกล้ถึงกำหนด |
| **Overdue** | 🔴 แดง | จำนวน PM ที่เลยกำหนดแล้ว |

#### 3.3.4 Machine Status Table (ตารางสถานะเครื่องจักร)

คอลัมน์ในตาราง:

| คอลัมน์ | คำอธิบาย |
|---------|---------|
| **No** | ลำดับที่ |
| **Machine Name** | ชื่อเครื่องจักร + รหัสเครื่อง |
| **Machine Type** | ประเภทเครื่องจักร (LSM, VNS, etc.) |
| **Area** | พื้นที่ติดตั้ง |
| **PM Type** | ประเภท PM (Weekly PM, Monthly PM, etc.) |
| **Last PM** | วันที่ทำ PM ล่าสุด (DD/MM/YYYY HH:mm) |
| **Next PM** | วันที่ต้องทำ PM ครั้งถัดไป |
| **Status** | สถานะปัจจุบัน (ดูตารางสถานะด้านล่าง) |
| **Action** | ปุ่ม Inspect และ History |

#### 3.3.5 ความหมายของสถานะ (Status)

| สถานะ | Badge | ความหมาย |
|-------|-------|---------|
| **Checked (ALL OK)** | 🟢 เขียว | PM ล่าสุดผ่านทุกรายการ |
| **Checked (HAVE NG)** | 🔴 แดง | PM ล่าสุดมีรายการไม่ผ่าน |
| **Upcoming** | 🟡 เหลือง | ใกล้ถึงกำหนด PM (ภายในจำนวนวันที่ตั้งแจ้งเตือน) |
| **Overdue** | 🔴 แดง | เลยกำหนด PM แล้ว |
| **Normal** | 🟢 เขียว | ยังไม่ถึงกำหนด PM |

#### 3.3.6 Action Buttons

| ปุ่ม | ไอคอน | คำอธิบาย |
|-----|------|---------|
| **Inspect** | 📋 | เข้าสู่หน้าทำ PM (หรือเลื่อนวัน ขึ้นอยู่กับสิทธิ์) |
| **History** | 🕐 | ดูประวัติ PM ของเครื่องนั้น |

> **📌 หมายเหตุ:** สำหรับผู้ใช้ที่มีสิทธิ์ `PM_AND_RESCHEDULE` เมื่อกดปุ่ม Inspect จะมี Popover ให้เลือกระหว่าง "ทำ PM" หรือ "เลื่อนกำหนด"

#### 3.3.7 Pagination

ด้านล่างตารางจะมี Pagination แสดง:
- หน้าปัจจุบัน / ทั้งหมด
- ปุ่ม Previous / Next
- ปุ่มเลขหน้า

### 3.4 Auto-Refresh

- Dashboard จะ Refresh อัตโนมัติทุก **5 นาที**
- ตัวนับเวลาแสดงที่มุมขวาบน (เช่น "Auto-refresh in: 4:30")
- สามารถกด **🔄 Refresh** เพื่อ Refresh ทันที (ตัวนับจะรีเซ็ต)

### 3.5 Notification Center

คลิกที่ 🔔 เพื่อดูรายการแจ้งเตือน:
- PM ที่ใกล้ถึงกำหนด
- PM ที่เลยกำหนดแล้ว
- สามารถคลิกที่รายการเพื่อไปยังหน้าทำ PM ได้

---

## 4. การจัดการเครื่องจักร (Machines)

### 4.1 URL การเข้าถึง

```
http://localhost:3000/machines
```

### 4.2 เมนูย่อย

| เมนู | URL | คำอธิบาย |
|-----|-----|---------|
| **ภาพรวม (Overall)** | `/machines/overall` | ดูเครื่องจักรแบบ Layout |
| **พื้นที่ (Areas)** | `/machines/areas` | จัดการพื้นที่ในโรงงาน |
| **ประเภทเครื่อง (Types)** | `/machines/machine-types` | จัดการประเภทเครื่องจักร |
| **Preventive Types** | `/machines/types` | จัดการประเภท PM และ Checklist |
| **Master** | `/machines/master` | จัดการ Machine Master |
| **ผู้ใช้งาน (Users)** | `/machines/users` | จัดการผู้ใช้งาน (Admin เท่านั้น) |

### 4.3 หน้าจัดการ Preventive Types

#### 4.3.1 รายการ Preventive Types

แสดงรายการประเภท PM ทั้งหมด เช่น:
- Weekly PM
- Monthly PM
- Yearly PM

#### 4.3.2 การจัดการ Checklist

แต่ละ Preventive Type มี Checklist ที่ต้องตรวจสอบ ประกอบด้วย:

| ฟิลด์ | คำอธิบาย |
|------|---------|
| **Topic** | หัวข้อที่ต้องตรวจ |
| **Description** | รายละเอียดเพิ่มเติม |
| **Type** | ประเภท: BOOLEAN (OK/NG) หรือ NUMERIC (ตัวเลข + min/max) |
| **Required** | จำเป็นต้องกรอก |
| **Order** | ลำดับการแสดง |

### 4.4 หน้าจัดการ Users (Admin เท่านั้น)

สำหรับ Admin สามารถ:
- ดูรายชื่อผู้ใช้ทั้งหมด
- เพิ่มผู้ใช้ใหม่
- แก้ไขข้อมูลผู้ใช้
- กำหนด Machine ที่แต่ละคนรับผิดชอบ
- ตั้งค่า System Role และ Permission Type

---

## 5. การทำ PM (PM Inspection)

### 5.1 URL การเข้าถึง

```
http://localhost:3000/pm/inspect/{machineId}?typeId={preventiveTypeId}
```

### 5.2 วิธีเข้าถึงหน้า PM Inspection

**วิธีที่ 1: จาก Dashboard**
1. ไปที่หน้า Dashboard
2. คลิกปุ่ม **"Inspect"** ที่เครื่องจักรที่ต้องการ

**วิธีที่ 2: จาก Calendar**
1. ไปที่หน้า Calendar
2. คลิกที่ Event PM ในวันที่ต้องการ

### 5.3 ส่วนประกอบของหน้า PM Inspection

#### 5.3.1 Header Section

แสดงข้อมูลเครื่องจักร:
- รหัสเครื่อง (Code)
- ชื่อเครื่อง
- รุ่น (Model)
- ตำแหน่ง (Location)
- ประเภท PM ที่กำลังทำ

#### 5.3.2 Checklist Section

แสดงรายการ Checklist ที่ต้องตรวจสอบ แบ่งเป็น:

| ประเภท | วิธีกรอก | ตัวอย่าง |
|--------|---------|---------|
| **BOOLEAN** | กดปุ่ม ✅ OK หรือ ❌ NG | ตรวจสอบความสะอาด |
| **NUMERIC** | กรอกตัวเลขในช่อง พร้อมแสดง Min/Max | วัดแรงดัน (5.0 - 10.0 bar) |

#### 5.3.3 การตรวจสอบค่า NUMERIC

| สถานะ | การแสดงผล | ความหมาย |
|-------|----------|---------|
| **ในช่วง** | 🟢 เขียว | ค่าอยู่ระหว่าง Min และ Max |
| **นอกช่วง** | 🔴 แดง | ค่าต่ำกว่า Min หรือสูงกว่า Max (NG อัตโนมัติ) |

#### 5.3.4 การถ่ายรูปประกอบ

แต่ละรายการ Checklist สามารถถ่ายรูป:

| ประเภท | คำอธิบาย | วิธีใช้ |
|--------|---------|--------|
| **Image Before** | รูปก่อนทำ PM | คลิก 📷 แล้วเลือก "Before" |
| **Image After** | รูปหลังทำ PM | คลิก 📷 แล้วเลือก "After" |

**วิธีถ่ายรูป:**
1. คลิกไอคอน 📷 ที่ต้องการ
2. เลือก **"ถ่ายรูปใหม่"** หรือ **"เลือกจากแกลเลอรี"**
3. ถ้าเลือกถ่ายรูปใหม่ กล้องจะเปิดขึ้น กดปุ่ม Capture
4. รูปจะแสดงใน Thumbnail

#### 5.3.5 Sub-Items (รายการย่อย)

บาง Checklist อาจมี Sub-Items เช่น:
- Fixture 1, Fixture 2, Fixture 3...

ระบบจะแสดง Sub-Items แบบแนวนอน ให้กรอกผลแต่ละตัว

#### 5.3.6 ส่วนสรุปและบันทึก

| ฟิลด์ | คำอธิบาย | จำเป็น |
|------|---------|--------|
| **ผู้ตรวจสอบ (Inspector)** | ชื่อผู้ทำ PM (Cleaning By) | ✅ จำเป็น |
| **ผู้ตรวจรับ (Checker)** | ชื่อผู้ตรวจรับ (Check By) | ❌ ไม่จำเป็น |
| **หมายเหตุ (Remark)** | หมายเหตุเพิ่มเติม | ❌ ไม่จำเป็น |

### 5.4 ขั้นตอนการทำ PM

1. **เข้าหน้า PM Inspection** ตามวิธีในข้อ 5.2
2. **ตรวจสอบข้อมูลเครื่อง** ที่แสดงด้านบนว่าถูกต้อง
3. **กรอก Checklist ทุกรายการ**
   - BOOLEAN: กดปุ่ม OK หรือ NG
   - NUMERIC: กรอกค่าที่วัดได้
4. **ถ่ายรูปประกอบ** (ถ้าจำเป็น)
5. **กรอกชื่อผู้ตรวจสอบ** ในฟิลด์ Inspector
6. **กรอกชื่อผู้ตรวจรับ** (ถ้ามี)
7. **เพิ่มหมายเหตุ** (ถ้ามี)
8. **กดปุ่ม "บันทึก"**
9. ระบบจะแสดง Popup ยืนยันความสำเร็จ

### 5.5 การแก้ไข PM Record ที่บันทึกแล้ว

1. ไปที่หน้า **PM History**
2. คลิกที่ Record ที่ต้องการแก้ไข
3. ระบบจะโหลดข้อมูลเดิมมาแสดง
4. แก้ไขข้อมูลที่ต้องการ
5. กดปุ่ม **"บันทึก"**

---

## 6. ประวัติ PM (PM History)

### 6.1 URL การเข้าถึง

```
http://localhost:3000/pm/history/{machineId}?typeId={preventiveTypeId}
```

### 6.2 วิธีเข้าถึง

1. จาก Dashboard: กดปุ่ม **"History"** ที่เครื่องจักร
2. จาก URL โดยตรง

### 6.3 ส่วนประกอบของหน้า PM History

#### 6.3.1 Filter Section

| ตัวกรอง | คำอธิบาย |
|--------|---------|
| **Year** | กรองตามปี |
| **PM Type** | กรองตามประเภท PM |
| **Area** | กรองตามพื้นที่ |
| **Machine Type** | กรองตามประเภทเครื่อง |

#### 6.3.2 Export Buttons

| ปุ่ม | คำอธิบาย |
|-----|---------|
| **📊 Export Excel (หน้านี้)** | Export เฉพาะหน้าปัจจุบัน |
| **📊 Export Excel (ทั้งหมด)** | Export ทุก Record ที่กรองได้ |
| **📄 Export PDF (หน้านี้)** | Export เป็น PDF เฉพาะหน้าปัจจุบัน |
| **📄 Export PDF (ทั้งหมด)** | Export เป็น PDF ทั้งหมด |

#### 6.3.3 History Table

ตารางแสดงประวัติ PM ประกอบด้วย:

| คอลัมน์ | คำอธิบาย |
|---------|---------|
| **วันที่** | วันที่ทำ PM |
| **ผู้ตรวจสอบ** | ชื่อผู้ทำ PM |
| **ผู้ตรวจรับ** | ชื่อผู้ตรวจรับ |
| **สถานะ** | ALL OK หรือ HAS NG |
| **[หัวข้อ Checklist]** | ผลการตรวจแต่ละหัวข้อ (✅/❌ หรือ ค่าตัวเลข) |
| **Action** | ปุ่มดู/แก้ไข/ลบ |

#### 6.3.4 การแสดงผล Checklist Values

| ประเภท | การแสดงผล |
|--------|----------|
| **BOOLEAN - OK** | ✅ เขียว |
| **BOOLEAN - NG** | ❌ แดง |
| **NUMERIC - ในช่วง** | ตัวเลข (เขียว) |
| **NUMERIC - นอกช่วง** | ตัวเลข (แดง) |
| **ไม่ได้กรอก** | - |

### 6.4 การดูรายละเอียด PM Record

1. คลิกที่แถว Record ที่ต้องการ
2. ระบบจะขยายแสดงรายละเอียด:
   - รูปภาพ Before/After
   - หมายเหตุแต่ละรายการ
   - Sub-Item Results

### 6.5 การลบ PM Record (Admin เท่านั้น)

1. คลิกปุ่ม **🗑️ ลบ** ที่ Record
2. ยืนยันการลบใน Popup
3. Record จะถูกลบออกจากระบบ

---

## 7. ปฏิทิน PM (Calendar)

### 7.1 URL การเข้าถึง

```
http://localhost:3000/calendar
```

### 7.2 ส่วนประกอบของหน้า Calendar

#### 7.2.1 Navigation

| องค์ประกอบ | คำอธิบาย |
|-----------|---------|
| **◀ ปุ่มซ้าย** | เดือนก่อนหน้า |
| **▶ ปุ่มขวา** | เดือนถัดไป |
| **ชื่อเดือน/ปี** | แสดงเดือนปัจจุบัน |
| **Today Button** | กลับไปเดือนปัจจุบัน |

#### 7.2.2 Calendar Grid

ปฏิทินแสดงแบบ Grid 7 วัน x 5-6 สัปดาห์

#### 7.2.3 Event Badges (สีของ Event)

| สี | ประเภท | ความหมาย |
|---|-------|---------|
| 🟢 **เขียว** | Completed | PM ที่ทำเสร็จแล้ว |
| 🔴 **แดง** | Overdue | PM ที่เลยกำหนด |
| 🟠 **ส้ม** | Has NG | PM ที่มีรายการไม่ผ่าน |
| 🟡 **เหลือง** | Upcoming | PM ที่ใกล้ถึงกำหนด |
| 🔵 **น้ำเงิน** | Scheduled | PM ที่กำหนดไว้ในอนาคต |

### 7.3 การใช้งานปฏิทิน

#### 7.3.1 การดู Events

- วันที่มี PM จะแสดง Badge สีตามสถานะ
- หากวันนั้นมีหลาย PM จะแสดงหลาย Badge
- คลิกที่วันที่จะแสดง Modal รายการ PM ของวันนั้น

#### 7.3.2 การ Click Event

1. คลิกที่ Event Badge ในปฏิทิน
2. ระบบจะทำงานตามสิทธิ์:
   - **PM_ONLY**: เข้าหน้าทำ PM โดยตรง
   - **RESCHEDULE_ONLY**: เปิด Modal เลื่อนกำหนด
   - **PM_AND_RESCHEDULE**: แสดง Popover ให้เลือก

#### 7.3.3 การเลื่อนกำหนด PM (Reschedule)

สำหรับผู้ที่มีสิทธิ์ Reschedule:

1. คลิก Event ที่ต้องการเลื่อน
2. เลือก **"เลื่อนกำหนด"** (ถ้ามี Popover)
3. Modal จะเปิดขึ้นแสดง:
   - ชื่อเครื่อง
   - ประเภท PM
   - วันที่ปัจจุบัน
   - ช่องเลือกวันใหม่
4. เลือกวันใหม่
5. กดปุ่ม **"บันทึก"**
6. ระบบจะอัปเดตกำหนด PM ใหม่

---

## 8. รายงาน (Reports)

### 8.1 URL การเข้าถึง

```
http://localhost:3000/reports
```

### 8.2 ตัวกรองรายงาน

| ตัวกรอง | คำอธิบาย |
|--------|---------|
| **Start Date** | วันที่เริ่มต้น |
| **End Date** | วันที่สิ้นสุด |
| **Machine** | เลือกเครื่องจักร |
| **PM Type** | เลือกประเภท PM |
| **Status** | เลือกสถานะ (Completed/Late) |

### 8.3 ผลลัพธ์รายงาน

ตารางแสดงรายการ PM Record ตามช่วงที่เลือก:

| คอลัมน์ | คำอธิบาย |
|---------|---------|
| **วันที่** | วันที่ทำ PM |
| **เครื่องจักร** | ชื่อ/รหัสเครื่อง |
| **ประเภท PM** | Weekly PM, Monthly PM, etc. |
| **ผู้ตรวจสอบ** | ชื่อผู้ทำ |
| **สถานะ** | ALL OK / HAS NG |

### 8.4 Export รายงาน

- **Export Excel**: ดาวน์โหลดไฟล์ .xlsx
- **Export PDF**: ดาวน์โหลดไฟล์ .pdf

---

## 9. การวิเคราะห์ (Analysis)

### 9.1 หน้าวิเคราะห์เครื่องจักร

```
http://localhost:3000/analysis/machine
```

แสดงสถิติ:
- จำนวน PM ที่ทำ
- อัตราการตรงเวลา
- อัตราการผ่าน/ไม่ผ่าน

### 9.2 หน้าวิเคราะห์ผู้ปฏิบัติงาน

```
http://localhost:3000/analysis/operator
```

แสดงสถิติแต่ละผู้ปฏิบัติงาน:
- จำนวน PM ที่ทำ
- อัตราการตรงเวลา
- ประสิทธิภาพการทำงาน

---

## 10. คำถามที่พบบ่อย (FAQ)

### ❓ ทำไมฉันเห็นเครื่องจักรไม่ครบ?

**คำตอบ:** ระบบจะแสดงเฉพาะเครื่องจักรที่คุณได้รับมอบหมาย (Assigned Machines)
- ติดต่อ Admin เพื่อขอเพิ่มสิทธิ์เข้าถึงเครื่อง

---

### ❓ ทำไมปุ่ม "Inspect" แสดง "เลื่อนวัน" แทน?

**คำตอบ:** คุณมีสิทธิ์ `RESCHEDULE_ONLY` ซึ่งสามารถเลื่อนกำหนด PM ได้อย่างเดียว
- ติดต่อ Admin เพื่อขอเปลี่ยนสิทธิ์เป็น `PM_AND_RESCHEDULE`

---

### ❓ ทำ PM แล้วแต่สถานะยังแสดงเป็น "Checked (HAVE NG)"?

**คำตอบ:** สถานะนี้หมายความว่า PM ครั้งล่าสุดมีรายการที่เลือก NG
- สถานะจะกลับเป็นปกติเมื่อทำ PM ครั้งถัดไปและผ่านทุกรายการ

---

### ❓ ค่า NUMERIC ที่กรอกเป็นสีแดง หมายความว่าอย่างไร?

**คำตอบ:** ค่าที่กรอกอยู่นอกช่วงที่กำหนด (Min/Max)
- ตรวจสอบค่า Min/Max ที่แสดงในช่อง
- ถ้าค่าถูกต้องจริงๆ แต่นอกช่วง ให้เพิ่มหมายเหตุอธิบาย

---

### ❓ ต้องการ Export รายงานย้อนหลังหลายปี ทำได้หรือไม่?

**คำตอบ:** ได้ ไปที่หน้า Reports หรือ PM History
- เลือกช่วงวันที่ที่ต้องการ
- กด "Export Excel (ทั้งหมด)" หรือ "Export PDF (ทั้งหมด)"

---

### ❓ ถ่ายรูปใน PM Inspection ไม่ได้ ทำอย่างไร?

**คำตอบ:** ตรวจสอบสิ่งต่อไปนี้:
1. Browser ได้รับอนุญาตใช้กล้องหรือไม่
2. เปิดใช้งาน HTTPS หรือ localhost
3. มีกล้องเชื่อมต่ออยู่หรือไม่

---

### ❓ ลืมรหัสผ่านทำอย่างไร?

**คำตอบ:** ติดต่อผู้ดูแลระบบ (Admin) เพื่อรีเซ็ตรหัสผ่าน

---

### ❓ ระบบ Auto-refresh ทุกกี่นาที?

**คำตอบ:** ทุก 5 นาที (300 วินาที)
- ดูตัวนับเวลาได้ที่มุมขวาบนของ Dashboard
- กด Refresh เพื่อ Refresh ทันทีได้

---

## 📞 ช่องทางติดต่อ

หากพบปัญหาในการใช้งาน กรุณาติดต่อ:

| ช่องทาง | รายละเอียด |
|--------|-----------|
| **ผู้ดูแลระบบ** | IT Department |
| **โทรศัพท์** | ภายใน xxxx |
| **อีเมล** | it-support@company.com |

---

<div align="center">

---

**© 2026 Maintenance PM Project**

เอกสารนี้เป็นความลับ ห้ามเผยแพร่โดยไม่ได้รับอนุญาต

---

</div>
