<div align="center">

# 🔧 คู่มือการแก้ไขปัญหา (Troubleshooting Guide)

## ระบบบริหารจัดการการบำรุงรักษาเชิงป้องกัน
## (Preventive Maintenance Management System)

---

**ชื่อโปรเจค:** Maintenance PM Project  
**เวอร์ชัน:** 1.0.0  
**วันที่จัดทำ:** 10 มกราคม 2569 (2026)  
**จัดทำโดย:** ทีมพัฒนาระบบ

---

</div>

---

## 📋 ข้อมูลเอกสาร

| รายการ | รายละเอียด |
|--------|-----------|
| **ชื่อเอกสาร** | คู่มือการแก้ไขปัญหา (Troubleshooting Guide) |
| **รหัสเอกสาร** | PM-DOC-TG-001 |
| **เวอร์ชัน** | 1.0.0 |
| **วันที่จัดทำ** | 10 มกราคม 2569 |
| **ปรับปรุงล่าสุด** | 10 มกราคม 2569 |
| **สถานะ** | เผยแพร่ (Released) |

---

## สารบัญ

1. [ปัญหาการเข้าสู่ระบบ (Login Issues)](#1-ปัญหาการเข้าสู่ระบบ-login-issues)
2. [ปัญหาการเชื่อมต่อ (Connection Issues)](#2-ปัญหาการเชื่อมต่อ-connection-issues)
3. [ปัญหาข้อมูลไม่แสดง (Data Display Issues)](#3-ปัญหาข้อมูลไม่แสดง-data-display-issues)
4. [ปัญหาการทำ PM (PM Inspection Issues)](#4-ปัญหาการทำ-pm-pm-inspection-issues)
5. [ปัญหาการ Export (Export Issues)](#5-ปัญหาการ-export-export-issues)
6. [ปัญหา Email Notification](#6-ปัญหา-email-notification)
7. [ปัญหา Server และ Database](#7-ปัญหา-server-และ-database)
8. [ปัญหา Performance](#8-ปัญหา-performance)
9. [Error Messages Reference](#9-error-messages-reference)
10. [การติดต่อขอความช่วยเหลือ](#10-การติดต่อขอความช่วยเหลือ)

---

## 1. ปัญหาการเข้าสู่ระบบ (Login Issues)

### 1.1 "Invalid credentials"

**อาการ:**
- กรอก Username และ Password แล้วกด Login แต่แสดงข้อความ "Invalid credentials"

**สาเหตุที่เป็นไปได้:**
1. Username หรือ Password ผิด
2. Caps Lock เปิดอยู่
3. มี Space ก่อน/หลัง Username

**วิธีแก้ไข:**
| ขั้นตอน | การดำเนินการ |
|--------|-------------|
| 1 | ตรวจสอบ Caps Lock |
| 2 | ลบ Username/Password แล้วพิมพ์ใหม่ |
| 3 | ติดต่อ Admin เพื่อ Reset Password |

---

### 1.2 "Connection Error"

**อาการ:**
- แสดงข้อความ "Connection Error" หรือ "Cannot connect to server"

**สาเหตุที่เป็นไปได้:**
1. Backend Server ไม่ทำงาน
2. Network ไม่เชื่อมต่อ
3. SSL Certificate ไม่ได้ยอมรับ

**วิธีแก้ไข:**
| ขั้นตอน | การดำเนินการ |
|--------|-------------|
| 1 | ตรวจสอบ Internet Connection |
| 2 | เปิด https://localhost:5006 แล้ว Accept Certificate |
| 3 | ติดต่อ IT เพื่อตรวจสอบ Server |

---

### 1.3 "Invalid or Expired token"

**อาการ:**
- เข้าสู่ระบบได้แต่ระบบแจ้ง "Invalid or Expired token"
- ถูก Redirect ไปหน้า Login

**สาเหตุที่เป็นไปได้:**
1. Token หมดอายุ
2. Login จากหลายอุปกรณ์

**วิธีแก้ไข:**
| ขั้นตอน | การดำเนินการ |
|--------|-------------|
| 1 | ล้าง Browser Cache และ Cookies |
| 2 | Login ใหม่อีกครั้ง |

**วิธีล้าง Cache:**
- **Chrome:** กด `Ctrl + Shift + Delete` → เลือก "Cookies and other site data" → Clear data
- **Edge:** กด `Ctrl + Shift + Delete` → เลือก "Cookies and other site data" → Clear now

---

### 1.4 หน้าจอค้าง "Signing in..."

**อาการ:**
- กด Login แล้วปุ่มแสดง "Signing in..." ค้างนาน

**สาเหตุที่เป็นไปได้:**
1. Network Timeout
2. Server ตอบสนองช้า

**วิธีแก้ไข:**
| ขั้นตอน | การดำเนินการ |
|--------|-------------|
| 1 | รอ 30 วินาที |
| 2 | Refresh หน้า (F5) |
| 3 | ตรวจสอบ Network Connection |

---

## 2. ปัญหาการเชื่อมต่อ (Connection Issues)

### 2.1 หน้า Dashboard ไม่โหลด

**อาการ:**
- เห็นหน้าขาวหรือ Loading ค้าง
- แสดงข้อความ "Failed to load data"

**วิธีแก้ไข:**
| ขั้นตอน | การดำเนินการ |
|--------|-------------|
| 1 | Refresh หน้า (F5) |
| 2 | ตรวจสอบ Backend Server ทำงานอยู่ |
| 3 | ตรวจสอบ Console (F12) เพื่อดู Error |

---

### 2.2 SSL Certificate Error

**อาการ:**
- Browser แสดง "Your connection is not private"
- แสดง "NET::ERR_CERT_AUTHORITY_INVALID"

**วิธีแก้ไข:**

**สำหรับ Chrome:**
1. เปิด `https://localhost:5006` โดยตรง
2. คลิก "Advanced"
3. คลิก "Proceed to localhost (unsafe)"

**สำหรับ Edge:**
1. เปิด `https://localhost:5006` โดยตรง
2. คลิก "Details"
3. คลิก "Go on to the webpage (Not recommended)"

---

### 2.3 CORS Error

**อาการ:**
- Console แสดง "Access-Control-Allow-Origin" error

**สาเหตุ:**
- Frontend URL ไม่ได้อยู่ใน CORS whitelist

**วิธีแก้ไข (สำหรับ Admin):**
ตรวจสอบไฟล์ `backend/server.js` ว่า CORS config ถูกต้อง

---

## 3. ปัญหาข้อมูลไม่แสดง (Data Display Issues)

### 3.1 ไม่เห็นเครื่องจักรใน Dashboard

**อาการ:**
- Dashboard ว่างเปล่า ไม่มีเครื่องจักร
- เห็นข้อความ "No machines found"

**สาเหตุที่เป็นไปได้:**
1. ไม่ได้รับมอบหมายเครื่องจักร
2. ตัวกรอง (Filter) ตั้งค่าที่ไม่มีข้อมูล

**วิธีแก้ไข:**
| ขั้นตอน | การดำเนินการ |
|--------|-------------|
| 1 | ตั้งค่า Filter เป็น "All" ทุกช่อง |
| 2 | ติดต่อ Admin เพื่อตรวจสอบการมอบหมายเครื่องจักร |

---

### 3.2 ข้อมูลไม่ Update

**อาการ:**
- ทำ PM แล้วแต่ Dashboard ไม่เปลี่ยน
- สถานะเครื่องไม่ Update

**วิธีแก้ไข:**
| ขั้นตอน | การดำเนินการ |
|--------|-------------|
| 1 | กดปุ่ม "Refresh" ที่มุมขวาบน |
| 2 | รอ Auto-refresh (ทุก 5 นาที) |
| 3 | Refresh หน้า (F5) |

---

### 3.3 รูปภาพไม่แสดง

**อาการ:**
- รูปเครื่องจักรหรือรูป PM ไม่แสดง
- แสดงเป็นไอคอน Broken Image

**สาเหตุที่เป็นไปได้:**
1. ไฟล์รูปถูกลบ
2. Path ไม่ถูกต้อง
3. Permission Error

**วิธีแก้ไข (สำหรับ Admin):**
| ขั้นตอน | การดำเนินการ |
|--------|-------------|
| 1 | ตรวจสอบโฟลเดอร์ `/backend/uploads/` |
| 2 | ตรวจสอบ Permission ของโฟลเดอร์ |

---

## 4. ปัญหาการทำ PM (PM Inspection Issues)

### 4.1 บันทึก PM ไม่ได้

**อาการ:**
- กดปุ่ม "บันทึก" แล้วไม่มีอะไรเกิดขึ้น
- แสดง Error Message

**สาเหตุที่เป็นไปได้:**
1. กรอกข้อมูลไม่ครบ (Required Fields)
2. Network Error
3. Session หมดอายุ

**วิธีแก้ไข:**
| ขั้นตอน | การดำเนินการ |
|--------|-------------|
| 1 | ตรวจสอบว่ากรอก Checklist ครบทุกข้อ |
| 2 | ตรวจสอบว่ากรอกชื่อ Inspector |
| 3 | Login ใหม่แล้วลองอีกครั้ง |

---

### 4.2 ถ่ายรูปไม่ได้

**อาการ:**
- กดปุ่มกล้องแล้วไม่มีอะไรเกิดขึ้น
- Browser ขอ Permission แล้วกดอนุญาตแต่ยังไม่ทำงาน

**สาเหตุที่เป็นไปได้:**
1. ไม่ได้อนุญาต Camera Permission
2. ไม่มี Webcam เชื่อมต่อ
3. ใช้ HTTP แทน HTTPS

**วิธีแก้ไข:**
| ขั้นตอน | การดำเนินการ |
|--------|-------------|
| 1 | ตรวจสอบว่ามี Webcam เชื่อมต่อ |
| 2 | ตรวจสอบ Permission ใน Browser Settings |
| 3 | ใช้ HTTPS (localhost หรือ https://) |

**ตรวจสอบ Camera Permission (Chrome):**
1. คลิกไอคอนกุญแจใน Address Bar
2. ดู "Camera" ต้องเป็น "Allow"

---

### 4.3 ค่า NUMERIC เป็นสีแดงทั้งที่กรอกถูกต้อง

**อาการ:**
- กรอกค่าตัวเลขแล้วแสดงสีแดง (NG)
- แต่ค่าที่กรอกอยู่ในช่วงที่ถูกต้อง

**สาเหตุที่เป็นไปได้:**
1. ค่า Min/Max ใน Checklist ตั้งไม่ถูกต้อง
2. กรอกค่ามีช่องว่างหรือตัวอักษร

**วิธีแก้ไข:**
| ขั้นตอน | การดำเนินการ |
|--------|-------------|
| 1 | ตรวจสอบว่ากรอกเป็นตัวเลขเท่านั้น |
| 2 | ตรวจสอบค่า Min/Max ที่แสดง |
| 3 | ติดต่อ Admin ถ้าค่า Min/Max ผิด |

---

## 5. ปัญหาการ Export (Export Issues)

### 5.1 Export Excel ไม่ได้

**อาการ:**
- กดปุ่ม Export แล้วไม่มีไฟล์ Download
- Browser แสดง Error

**วิธีแก้ไข:**
| ขั้นตอน | การดำเนินการ |
|--------|-------------|
| 1 | ตรวจสอบว่า Popup Blocker ไม่ได้บล็อก |
| 2 | ตรวจสอบโฟลเดอร์ Downloads |
| 3 | ลองใช้ Browser อื่น |

---

### 5.2 ไฟล์ Excel เปิดไม่ได้

**อาการ:**
- Download ไฟล์สำเร็จแต่เปิดไม่ได้
- Excel แสดง Error

**วิธีแก้ไข:**
| ขั้นตอน | การดำเนินการ |
|--------|-------------|
| 1 | ตรวจสอบว่ามี Microsoft Excel หรือ LibreOffice |
| 2 | ลอง Export ใหม่ |
| 3 | ลองใช้ Google Sheets เปิด |

---

## 6. ปัญหา Email Notification

### 6.1 ไม่ได้รับ Email แจ้งเตือน

**สาเหตุที่เป็นไปได้:**
1. Email ไปอยู่ใน Spam/Junk
2. SMTP ไม่ได้ตั้งค่า
3. Email Recipient ผิด

**วิธีแก้ไข:**
| ขั้นตอน | การดำเนินการ |
|--------|-------------|
| 1 | ตรวจสอบ Spam/Junk Folder |
| 2 | ตรวจสอบ Email Address ใน Profile |
| 3 | ติดต่อ Admin เพื่อตรวจสอบ SMTP Config |

---

## 7. ปัญหา Server และ Database

### 7.1 "Server is not responding"

**สำหรับ Admin:**

**ตรวจสอบ Backend:**
```bash
cd backend
npm start
```

**ตรวจสอบ Port:**
```bash
netstat -an | findstr :5006
```

---

### 7.2 "Database connection failed"

**สำหรับ Admin:**

**ตรวจสอบ SQL Server Service:**
```bash
services.msc
# ดูว่า "SQL Server (MSSQLSERVER)" เป็น Running
```

**ตรวจสอบ Connection String:**
```bash
npx prisma db pull
```

---

### 7.3 "EADDRINUSE: address already in use"

**อาการ:**
- ไม่สามารถ Start Backend ได้
- แสดง Error "EADDRINUSE :::5006"

**วิธีแก้ไข:**
```bash
# หา Process ที่ใช้ Port
netstat -ano | findstr :5006

# Kill Process
taskkill /PID <PID> /F
```

---

## 8. ปัญหา Performance

### 8.1 ระบบช้า

**สาเหตุที่เป็นไปได้:**
1. Database มีข้อมูลมาก
2. Server Resources ไม่พอ
3. Network ช้า

**วิธีแก้ไข (สำหรับ Admin):**
| การดำเนินการ | ผลลัพธ์ |
|-------------|--------|
| เพิ่ม Index ใน Database | Query เร็วขึ้น |
| เพิ่ม RAM/CPU | Processing เร็วขึ้น |
| ใช้ Pagination | Load ทีละน้อย |

---

### 8.2 Dashboard โหลดนาน

**วิธีแก้ไข:**
| ขั้นตอน | การดำเนินการ |
|--------|-------------|
| 1 | ใช้ Filter เพื่อจำกัดข้อมูล |
| 2 | ลด Items Per Page |

---

## 9. Error Messages Reference

### 9.1 รายการ Error Messages ที่พบบ่อย

| Error Message | ความหมาย | วิธีแก้ไข |
|---------------|---------|---------|
| Invalid credentials | Username/Password ผิด | ตรวจสอบและลองใหม่ |
| No token provided | ไม่ได้ Login | Login ก่อนใช้งาน |
| Invalid or Expired token | Token ไม่ถูกต้อง | Login ใหม่ |
| Access denied | ไม่มีสิทธิ์ | ติดต่อ Admin |
| Not found | ไม่พบข้อมูล | ตรวจสอบ ID ที่ขอ |
| Internal server error | Server มีปัญหา | ติดต่อ IT |
| Connection refused | ไม่สามารถเชื่อมต่อได้ | ตรวจสอบ Server |

---

## 10. การติดต่อขอความช่วยเหลือ

### 10.1 ก่อนติดต่อ กรุณาเตรียมข้อมูลดังนี้

| ข้อมูล | ตัวอย่าง |
|--------|---------|
| **ชื่อผู้แจ้ง** | นายสมชาย ใจดี |
| **แผนก** | Production |
| **อาการที่พบ** | Login ไม่ได้ แสดง "Invalid credentials" |
| **ขั้นตอนที่ทำ** | กรอก Username "somchai" Password "1234" กด Sign In |
| **Browser ที่ใช้** | Chrome 120 |
| **วันเวลาที่พบปัญหา** | 10/01/2569 09:30 |
| **Screenshot (ถ้ามี)** | แนบรูป Error |

### 10.2 ช่องทางติดต่อ

| ช่องทาง | รายละเอียด | เวลาทำการ |
|--------|-----------|----------|
| **โทรศัพท์** | ภายใน xxxx | 08:00 - 17:00 |
| **อีเมล** | it-support@company.com | 24 ชั่วโมง |
| **Help Desk** | ห้อง IT ชั้น 2 | 08:00 - 17:00 |

### 10.3 ระดับความเร่งด่วน

| ระดับ | คำอธิบาย | เวลาตอบสนอง |
|------|---------|------------|
| 🔴 **Critical** | ระบบใช้งานไม่ได้เลย | ภายใน 1 ชั่วโมง |
| 🟠 **High** | ฟังก์ชันหลักใช้ไม่ได้ | ภายใน 4 ชั่วโมง |
| 🟡 **Medium** | มีปัญหาแต่ยังใช้งานได้ | ภายใน 8 ชั่วโมง |
| 🟢 **Low** | คำถาม/ข้อเสนอแนะ | ภายใน 24 ชั่วโมง |

---

<div align="center">

---

**© 2026 Maintenance PM Project**

เอกสารนี้เป็นความลับ ห้ามเผยแพร่โดยไม่ได้รับอนุญาต

---

</div>
