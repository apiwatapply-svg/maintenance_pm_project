<div align="center">

# 📄 API Documentation

## ระบบบริหารจัดการการบำรุงรักษาเชิงป้องกัน
## (Preventive Maintenance Management System)

---

**ชื่อโปรเจค:** Maintenance PM Project  
**เวอร์ชัน:** 1.0.0  
**วันที่จัดทำ:** 10 มกราคม 2569 (2026)  
**จัดทำโดย:** ทีมพัฒนาระบบ

---

</div>

---

## 📋 ข้อมูลเอกสาร

| รายการ | รายละเอียด |
|--------|-----------|
| **ชื่อเอกสาร** | API Documentation |
| **รหัสเอกสาร** | PM-DOC-API-001 |
| **เวอร์ชัน** | 1.0.0 |
| **วันที่จัดทำ** | 10 มกราคม 2569 |
| **ปรับปรุงล่าสุด** | 10 มกราคม 2569 |
| **สถานะ** | เผยแพร่ (Released) |

---

## 📝 ประวัติการแก้ไข

| เวอร์ชัน | วันที่ | ผู้แก้ไข | รายละเอียดการแก้ไข |
|---------|--------|---------|-------------------|
| 1.0.0 | 10/01/2569 | ทีมพัฒนา | เอกสารฉบับแรก |

---

## สารบัญ

1. [บทนำ](#1-บทนำ)
2. [ข้อมูลทั่วไป](#2-ข้อมูลทั่วไป)
3. [การยืนยันตัวตน (Authentication)](#3-การยืนยันตัวตน-authentication)
4. [API Endpoints](#4-api-endpoints)
   - 4.1 [Authentication API](#41-authentication-api)
   - 4.2 [User Master API](#42-user-master-api)
   - 4.3 [Machine API](#43-machine-api)
   - 4.4 [Machine Master API](#44-machine-master-api)
   - 4.5 [Machine Type API](#45-machine-type-api)
   - 4.6 [Area API](#46-area-api)
   - 4.7 [Preventive Type API](#47-preventive-type-api)
   - 4.8 [PM Records API](#48-pm-records-api)
   - 4.9 [Dashboard API](#49-dashboard-api)
   - 4.10 [Report API](#410-report-api)
   - 4.11 [Upload API](#411-upload-api)
5. [รหัสสถานะ HTTP](#5-รหัสสถานะ-http)
6. [ข้อผิดพลาดที่พบบ่อย](#6-ข้อผิดพลาดที่พบบ่อย)
7. [ภาคผนวก](#7-ภาคผนวก)

---

## 1. บทนำ

### 1.1 วัตถุประสงค์

เอกสารฉบับนี้จัดทำขึ้นเพื่ออธิบายรายละเอียดของ API (Application Programming Interface) ทั้งหมดที่ใช้ในระบบบริหารจัดการการบำรุงรักษาเชิงป้องกัน (Preventive Maintenance Management System) เพื่อให้ผู้พัฒนาหรือผู้ที่เกี่ยวข้องสามารถเชื่อมต่อและใช้งาน API ได้อย่างถูกต้อง

### 1.2 ขอบเขต

เอกสารนี้ครอบคลุม API ทั้งหมดที่ให้บริการโดยระบบ Backend ซึ่งประกอบด้วย:
- การจัดการผู้ใช้งาน (User Management)
- การจัดการเครื่องจักร (Machine Management)
- การจัดการแผนบำรุงรักษา (PM Plan Management)
- การบันทึกผลการบำรุงรักษา (PM Recording)
- การดึงรายงานและสถิติ (Reports & Dashboard)

### 1.3 กลุ่มผู้ใช้งานเอกสาร

- นักพัฒนาระบบ (Developers)
- ผู้ดูแลระบบ (System Administrators)
- ผู้ทดสอบระบบ (QA/Testers)

---

## 2. ข้อมูลทั่วไป

### 2.1 Base URL

```
Production:  https://{hostname}:5006/api
Development: http://localhost:5006/api
```

### 2.2 รูปแบบ Request/Response

| รายการ | รายละเอียด |
|--------|-----------|
| **Protocol** | HTTPS (Production) / HTTP (Development) |
| **Port** | 5006 |
| **Content-Type** | `application/json` |
| **Character Encoding** | UTF-8 |
| **Date Format** | ISO 8601 (YYYY-MM-DDTHH:mm:ss.sssZ) |

### 2.3 Response Format มาตรฐาน

**Success Response:**
```json
{
  "data": { ... },
  "message": "Success"
}
```

**Error Response:**
```json
{
  "error": "รายละเอียดข้อผิดพลาด"
}
```

---

## 3. การยืนยันตัวตน (Authentication)

### 3.1 วิธีการยืนยันตัวตน

ระบบใช้ **JWT (JSON Web Token)** สำหรับการยืนยันตัวตน โดยต้องส่ง Token ไปใน HTTP Header ดังนี้:

```
Authorization: Bearer <token>
```

### 3.2 การขอ Token

เรียก API `/api/auth/login` เพื่อขอ Token โดยส่ง username และ password

### 3.3 อายุการใช้งาน Token

Token จะมีอายุการใช้งานตลอดจนกว่าจะ Logout

### 3.4 ระดับสิทธิ์ผู้ใช้งาน

| ระดับ | รหัส | คำอธิบาย |
|-------|------|---------|
| ผู้ดูแลระบบ | `ADMIN` | เข้าถึงทุกฟังก์ชัน |
| ผู้ใช้งานทั่วไป | `USER` | เข้าถึงตามสิทธิ์ที่กำหนด |

---

## 4. API Endpoints

### 4.1 Authentication API

#### 4.1.1 เข้าสู่ระบบ (Login)

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/auth/login` |
| **Method** | `POST` |
| **ต้องยืนยันตัวตน** | ❌ ไม่ต้อง |

**Request Body:**
```json
{
  "username": "admin",
  "password": "password123"
}
```

**Response สำเร็จ (200 OK):**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "username": "admin",
    "name": "ผู้ดูแลระบบ",
    "systemRole": "ADMIN",
    "permissionType": "ALL",
    "role": "Admin"
  }
}
```

**Response ผิดพลาด (401 Unauthorized):**
```json
{
  "error": "Invalid credentials"
}
```

---

#### 4.1.2 ดึงข้อมูลผู้ใช้ปัจจุบัน

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/auth/me` |
| **Method** | `GET` |
| **ต้องยืนยันตัวตน** | ✅ ต้อง |

**Response สำเร็จ (200 OK):**
```json
{
  "id": 1,
  "username": "admin",
  "name": "ผู้ดูแลระบบ",
  "systemRole": "ADMIN",
  "permissionType": "ALL",
  "role": "Admin",
  "assignedMachines": [
    { "id": 1, "name": "LSM-001", "code": "LSM-001" }
  ]
}
```

---

### 4.2 User Master API

#### 4.2.1 ดึงรายชื่อผู้ใช้ทั้งหมด

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/user-master` |
| **Method** | `GET` |
| **ต้องยืนยันตัวตน** | ✅ ต้อง |

**Query Parameters:**
| พารามิเตอร์ | ประเภท | จำเป็น | คำอธิบาย |
|------------|--------|--------|---------|
| machineId | Integer | ❌ | กรองตามเครื่องจักรที่ได้รับมอบหมาย |

**Response สำเร็จ (200 OK):**
```json
[
  {
    "id": 1,
    "name": "สมชาย ใจดี",
    "role": "INSPECTOR",
    "employeeId": "EMP001",
    "email": "somchai@company.com",
    "username": "somchai",
    "systemRole": "USER",
    "permissionType": "PM_ONLY",
    "assignedMachines": [
      { "id": 1, "name": "LSM-001", "code": "LSM-001" }
    ]
  }
]
```

---

#### 4.2.2 สร้างผู้ใช้ใหม่

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/user-master` |
| **Method** | `POST` |
| **ต้องยืนยันตัวตน** | ✅ ต้อง (ADMIN เท่านั้น) |

**Request Body:**
```json
{
  "name": "พนักงานใหม่",
  "role": "INSPECTOR",
  "employeeId": "EMP003",
  "email": "new@company.com",
  "username": "newuser",
  "password": "password123",
  "systemRole": "USER",
  "permissionType": "PM_ONLY",
  "assignedMachineIds": [1, 2, 3]
}
```

| ฟิลด์ | ประเภท | จำเป็น | คำอธิบาย |
|-------|--------|--------|---------|
| name | String | ✅ | ชื่อ-นามสกุล |
| role | String | ✅ | บทบาท: INSPECTOR, CHECKER, BOTH |
| employeeId | String | ❌ | รหัสพนักงาน |
| email | String | ❌ | อีเมล |
| username | String | ❌ | ชื่อผู้ใช้สำหรับเข้าสู่ระบบ |
| password | String | ❌ | รหัสผ่าน |
| systemRole | String | ❌ | สิทธิ์ระบบ: USER, ADMIN (ค่าเริ่มต้น: USER) |
| permissionType | String | ❌ | ประเภทสิทธิ์: PM_ONLY, RESCHEDULE_ONLY, PM_AND_RESCHEDULE |
| assignedMachineIds | Array | ❌ | รายการ ID เครื่องจักรที่มอบหมาย |

**Response สำเร็จ (200 OK):**
```json
{
  "id": 3,
  "name": "พนักงานใหม่",
  "role": "INSPECTOR",
  "employeeId": "EMP003",
  "email": "new@company.com",
  "username": "newuser",
  "systemRole": "USER",
  "permissionType": "PM_ONLY",
  "assignedMachines": [
    { "id": 1, "name": "LSM-001", "code": "LSM-001" }
  ]
}
```

---

#### 4.2.3 แก้ไขข้อมูลผู้ใช้

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/user-master/:id` |
| **Method** | `PUT` |
| **ต้องยืนยันตัวตน** | ✅ ต้อง (ADMIN เท่านั้น) |

**Path Parameters:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
|------------|--------|---------|
| id | Integer | ID ของผู้ใช้ |

**Request Body:** เหมือนกับการสร้างผู้ใช้ใหม่

**Response สำเร็จ (200 OK):** เหมือนกับการสร้างผู้ใช้ใหม่

---

#### 4.2.4 ลบผู้ใช้

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/user-master/:id` |
| **Method** | `DELETE` |
| **ต้องยืนยันตัวตน** | ✅ ต้อง (ADMIN เท่านั้น) |

**Response สำเร็จ (200 OK):**
```json
{
  "message": "Deleted"
}
```

---

### 4.3 Machine API

#### 4.3.1 ดึงรายการเครื่องจักรทั้งหมด

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/machines` |
| **Method** | `GET` |
| **ต้องยืนยันตัวตน** | ✅ ต้อง |

**Response สำเร็จ (200 OK):**
```json
[
  {
    "id": 1,
    "code": "LSM-001",
    "name": "Laser Marker 001",
    "model": "LM-2000",
    "location": "Bay A1",
    "image": "/uploads/lsm001.jpg",
    "createdAt": "2025-01-01T00:00:00.000Z",
    "updatedAt": "2026-01-09T10:30:00.000Z",
    "pmPlans": [
      {
        "id": 1,
        "preventiveTypeId": 1,
        "frequencyDays": 7,
        "advanceNotifyDays": 2,
        "lastPMDate": "2026-01-05T00:00:00.000Z",
        "nextPMDate": "2026-01-12T00:00:00.000Z",
        "lastCheckStatus": "ALL_OK",
        "preventiveType": {
          "id": 1,
          "name": "Weekly PM"
        }
      }
    ],
    "machineMaster": {
      "id": 1,
      "code": "MC-LSM",
      "name": "Laser Marker",
      "machineType": {
        "id": 1,
        "name": "LSM",
        "area": {
          "id": 1,
          "name": "CLASS100"
        }
      }
    }
  }
]
```

---

#### 4.3.2 ดึงข้อมูลเครื่องจักรตาม ID

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/machines/:id` |
| **Method** | `GET` |
| **ต้องยืนยันตัวตน** | ❌ ไม่ต้อง |

**Path Parameters:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
|------------|--------|---------|
| id | Integer | ID ของเครื่องจักร |

---

#### 4.3.3 สร้างเครื่องจักรใหม่

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/machines` |
| **Method** | `POST` |
| **ต้องยืนยันตัวตน** | ❌ ไม่ต้อง |

**Request Body:**
```json
{
  "code": "LSM-010",
  "name": "Laser Marker 010",
  "model": "LM-3000",
  "location": "Bay B2",
  "machineMasterId": 1
}
```

---

#### 4.3.4 แก้ไขเครื่องจักร

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/machines/:id` |
| **Method** | `PUT` |
| **ต้องยืนยันตัวตน** | ❌ ไม่ต้อง |

---

#### 4.3.5 ลบเครื่องจักร

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/machines/:id` |
| **Method** | `DELETE` |
| **ต้องยืนยันตัวตน** | ❌ ไม่ต้อง |

---

### 4.4 Machine Master API

#### 4.4.1 ดึงรายการ Machine Master

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/machine-master` |
| **Method** | `GET` |
| **ต้องยืนยันตัวตน** | ✅ ต้อง |

**Response สำเร็จ (200 OK):**
```json
[
  {
    "id": 1,
    "code": "MC-LSM",
    "name": "Laser Marker",
    "description": "เครื่องยิงเลเซอร์",
    "machineTypeId": 1,
    "machineType": {
      "id": 1,
      "name": "LSM"
    }
  }
]
```

---

### 4.5 Machine Type API

#### 4.5.1 ดึงประเภทเครื่องจักรทั้งหมด

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/machine-types` |
| **Method** | `GET` |
| **ต้องยืนยันตัวตน** | ✅ ต้อง |

**Response สำเร็จ (200 OK):**
```json
[
  {
    "id": 1,
    "name": "LSM",
    "description": "Laser Marker",
    "areaId": 1,
    "area": {
      "id": 1,
      "name": "CLASS100"
    }
  }
]
```

---

### 4.6 Area API

#### 4.6.1 ดึงรายการพื้นที่ทั้งหมด

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/areas` |
| **Method** | `GET` |
| **ต้องยืนยันตัวตน** | ✅ ต้อง |

**Response สำเร็จ (200 OK):**
```json
[
  {
    "id": 1,
    "name": "CLASS100",
    "description": "พื้นที่ Class 100 - Clean Room",
    "machineTypes": [
      { "id": 1, "name": "LSM", "description": "Laser Marker" }
    ]
  }
]
```

---

### 4.7 Preventive Type API

#### 4.7.1 ดึงประเภท PM ทั้งหมด

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/preventive-types` |
| **Method** | `GET` |
| **ต้องยืนยันตัวตน** | ✅ ต้อง |

**Response สำเร็จ (200 OK):**
```json
[
  {
    "id": 1,
    "name": "Weekly PM",
    "description": "การตรวจสอบรายสัปดาห์",
    "isFixedDate": true,
    "postponeLogic": "SHIFT",
    "emailRecipients": "supervisor@company.com",
    "notifyAdvanceDays": 3,
    "masterChecklists": [
      {
        "id": 1,
        "topic": "ตรวจสอบความสะอาด",
        "type": "BOOLEAN",
        "order": 1
      }
    ]
  }
]
```

---

#### 4.7.2 สร้างประเภท PM

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/preventive-types` |
| **Method** | `POST` |
| **ต้องยืนยันตัวตน** | ❌ ไม่ต้อง |

**Request Body:**
```json
{
  "name": "Daily PM",
  "description": "การตรวจสอบรายวัน",
  "isFixedDate": true,
  "postponeLogic": "SHIFT",
  "emailRecipients": "team@company.com",
  "notifyAdvanceDays": 1
}
```

---

#### 4.7.3 เพิ่ม Master Checklist

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/preventive-types/:id/checklists` |
| **Method** | `POST` |
| **ต้องยืนยันตัวตน** | ❌ ไม่ต้อง |

**Request Body:**
```json
{
  "topic": "ตรวจสอบหัวข้อใหม่",
  "description": "คำอธิบาย",
  "type": "BOOLEAN",
  "isRequired": true,
  "order": 1
}
```

| ฟิลด์ | ประเภท | ค่าที่รับ | คำอธิบาย |
|-------|--------|----------|---------|
| type | String | BOOLEAN, NUMERIC | ประเภทการตรวจสอบ |

---

### 4.8 PM Records API

#### 4.8.1 ดึงตาราง PM (Schedule)

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/pm/schedule` |
| **Method** | `GET` |
| **ต้องยืนยันตัวตน** | ✅ ต้อง |

**Query Parameters:**
| พารามิเตอร์ | ประเภท | จำเป็น | คำอธิบาย |
|------------|--------|--------|---------|
| month | Integer | ✅ | เดือน (1-12) |
| year | Integer | ✅ | ปี ค.ศ. |

**Response สำเร็จ (200 OK):**
```json
[
  {
    "id": "record-1",
    "type": "completed",
    "date": "2026-01-05T10:30:00.000Z",
    "machine": {
      "id": 1,
      "name": "LSM-001",
      "code": "LSM-001"
    },
    "preventiveType": {
      "name": "Weekly PM"
    },
    "status": "COMPLETED",
    "lastCheckStatus": "ALL_OK",
    "inspector": "สมชาย ใจดี"
  }
]
```

---

#### 4.8.2 บันทึกผล PM

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/pm/record` |
| **Method** | `POST` |
| **ต้องยืนยันตัวตน** | ✅ ต้อง |

**Request Body:**
```json
{
  "machineId": 1,
  "preventiveTypeId": 1,
  "inspector": "สมชาย ใจดี",
  "checker": "สมหญิง รักงาน",
  "status": "COMPLETED",
  "remark": "ทำ PM สำเร็จ",
  "details": [
    {
      "checklistId": 1,
      "isPass": true,
      "value": null,
      "remark": null
    }
  ]
}
```

| ฟิลด์ | ประเภท | จำเป็น | คำอธิบาย |
|-------|--------|--------|---------|
| machineId | Integer | ✅ | ID เครื่องจักร |
| preventiveTypeId | Integer | ✅ | ID ประเภท PM |
| inspector | String | ❌ | ผู้ตรวจสอบ |
| checker | String | ❌ | ผู้ตรวจรับ |
| status | String | ✅ | สถานะ: COMPLETED, LATE |
| details | Array | ✅ | รายการผลการตรวจสอบ |

---

#### 4.8.3 ดึงประวัติ PM ของเครื่องจักร

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/pm/machine/:machineId/history` |
| **Method** | `GET` |
| **ต้องยืนยันตัวตน** | ✅ ต้อง |

**Query Parameters:**
| พารามิเตอร์ | ประเภท | จำเป็น | ค่าเริ่มต้น | คำอธิบาย |
|------------|--------|--------|-----------|---------|
| page | Integer | ❌ | 1 | หน้าที่ต้องการ |
| limit | Integer | ❌ | 10 | จำนวนรายการต่อหน้า |
| year | Integer | ❌ | - | กรองตามปี |
| pmTypeId | Integer | ❌ | - | กรองตามประเภท PM |

**Response สำเร็จ (200 OK):**
```json
{
  "data": [ ... ],
  "pagination": {
    "total": 25,
    "page": 1,
    "limit": 10,
    "totalPages": 3
  }
}
```

---

#### 4.8.4 เลื่อนกำหนด PM

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/pm/reschedule` |
| **Method** | `POST` |
| **ต้องยืนยันตัวตน** | ✅ ต้อง |

**Request Body:**
```json
{
  "machineId": 1,
  "preventiveTypeId": 1,
  "newDate": "2026-01-15"
}
```

---

### 4.9 Dashboard API

#### 4.9.1 ดึงสถิติ Dashboard

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/dashboard/stats` |
| **Method** | `GET` |
| **ต้องยืนยันตัวตน** | ✅ ต้อง |

**Response สำเร็จ (200 OK):**
```json
{
  "summary": {
    "completed": 45,
    "upcoming": 10,
    "overdue": 3,
    "has_ng": 2,
    "total": 60
  },
  "machines": [ ... ]
}
```

---

### 4.10 Report API

#### 4.10.1 ดึงรายงาน

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/reports` |
| **Method** | `GET` |
| **ต้องยืนยันตัวตน** | ✅ ต้อง |

**Query Parameters:**
| พารามิเตอร์ | ประเภท | จำเป็น | คำอธิบาย |
|------------|--------|--------|---------|
| startDate | String | ❌ | วันที่เริ่มต้น (YYYY-MM-DD) |
| endDate | String | ❌ | วันที่สิ้นสุด (YYYY-MM-DD) |
| machineId | Integer | ❌ | กรองตามเครื่องจักร |
| status | String | ❌ | กรองตามสถานะ |

---

### 4.11 Upload API

#### 4.11.1 อัพโหลดรูปภาพ

| รายการ | รายละเอียด |
|--------|-----------|
| **URL** | `/api/upload` |
| **Method** | `POST` |
| **ต้องยืนยันตัวตน** | ❌ ไม่ต้อง |
| **Content-Type** | `multipart/form-data` |

**Form Data:**
| ฟิลด์ | ประเภท | คำอธิบาย |
|-------|--------|---------|
| image | File | ไฟล์รูปภาพ (jpg, png, gif) |

**Response สำเร็จ (200 OK):**
```json
{
  "url": "/uploads/1704788800000-image.jpg"
}
```

---

## 5. รหัสสถานะ HTTP

| รหัส | สถานะ | คำอธิบาย |
|------|-------|---------|
| 200 | OK | ดำเนินการสำเร็จ |
| 201 | Created | สร้างข้อมูลสำเร็จ |
| 400 | Bad Request | ข้อมูล Request ไม่ถูกต้อง |
| 401 | Unauthorized | ไม่ได้ยืนยันตัวตน หรือ Token ไม่ถูกต้อง |
| 403 | Forbidden | ไม่มีสิทธิ์เข้าถึง |
| 404 | Not Found | ไม่พบข้อมูลที่ร้องขอ |
| 500 | Internal Server Error | เกิดข้อผิดพลาดภายในเซิร์ฟเวอร์ |

---

## 6. ข้อผิดพลาดที่พบบ่อย

### 6.1 Authentication Errors

| ข้อผิดพลาด | สาเหตุ | วิธีแก้ไข |
|-----------|-------|---------|
| `Invalid credentials` | Username หรือ Password ไม่ถูกต้อง | ตรวจสอบข้อมูลการเข้าสู่ระบบ |
| `Invalid or Expired token` | Token หมดอายุหรือไม่ถูกต้อง | ขอ Token ใหม่ |
| `No token provided` | ไม่ได้ส่ง Token มาใน Header | เพิ่ม Authorization Header |

### 6.2 Validation Errors

| ข้อผิดพลาด | สาเหตุ | วิธีแก้ไข |
|-----------|-------|---------|
| `Employee ID already exists` | รหัสพนักงานซ้ำ | ใช้รหัสพนักงานอื่น |
| `Username already exists` | Username ซ้ำ | ใช้ Username อื่น |
| `Access denied to this machine` | ไม่มีสิทธิ์เข้าถึงเครื่องจักรนี้ | ติดต่อ Admin เพื่อขอสิทธิ์ |

---

## 7. ภาคผนวก

### 7.1 ค่าคงที่ในระบบ

**System Role:**
| ค่า | คำอธิบาย |
|-----|---------|
| `ADMIN` | ผู้ดูแลระบบ - เข้าถึงได้ทุกฟังก์ชัน |
| `USER` | ผู้ใช้งานทั่วไป - เข้าถึงตามสิทธิ์ |

**Permission Type:**
| ค่า | คำอธิบาย |
|-----|---------|
| `PM_ONLY` | ทำ PM ได้อย่างเดียว |
| `RESCHEDULE_ONLY` | เลื่อนกำหนด PM ได้อย่างเดียว |
| `PM_AND_RESCHEDULE` | ทำ PM และเลื่อนกำหนดได้ |

**User Role:**
| ค่า | คำอธิบาย |
|-----|---------|
| `INSPECTOR` | ผู้ตรวจสอบ (Cleaning By) |
| `CHECKER` | ผู้ตรวจรับ (Check By) |
| `BOTH` | ทั้งสองบทบาท |

**PM Status:**
| ค่า | คำอธิบาย |
|-----|---------|
| `PLANNED` | วางแผนแล้ว |
| `COMPLETED` | เสร็จสิ้น |
| `LATE` | เลยกำหนด |

**Schedule Status:**
| ค่า | คำอธิบาย |
|-----|---------|
| `OK` | ปกติ |
| `UPCOMING` | ใกล้ถึงกำหนด |
| `OVERDUE` | เลยกำหนด |

**Last Check Status:**
| ค่า | คำอธิบาย |
|-----|---------|
| `ALL_OK` | ผลการตรวจสอบผ่านทั้งหมด |
| `HAS_NG` | มีรายการไม่ผ่าน |

---

<div align="center">

---

**© 2026 Maintenance PM Project**

เอกสารนี้เป็นความลับ ห้ามเผยแพร่โดยไม่ได้รับอนุญาต

---

</div>
